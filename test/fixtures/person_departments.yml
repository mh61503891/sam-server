<% people = YAML::load(ERB.new(IO.read('test/fixtures/people.yml')).result) %>
<% departments = YAML::load(ERB.new(IO.read('test/fixtures/departments.yml')).result) %>
<% titles = YAML::load(ERB.new(IO.read('test/fixtures/titles.yml')).result) %>

<% people.keys.each do |person| %>
  <% departments.keys.sample(Random.rand(0..5)).each do |department| %>
    <% titles.keys.sample(Random.rand(0..3)).each do |title| %>
person_deaprtment_<%= person %>_<%= department %>_<%= title %>:
  person: <%= person %>
  department: <%= department %>
  title: <%= title %>
    <% end %>
  <% end %>
<% end %>
